# Docker 环境变量配置示例
# 复制此文件为 .env 并修改相应的值

# 应用端口（默认 3000）
APP_PORT=3000

# 后端 API 地址
# 容器化部署配置选项：
# 1. 同一 docker-compose: http://backend:9080 (使用服务名)
# 2. 同一 Docker 网络: http://container-name:9080 (使用容器名)
# 3. 使用宿主机: http://host.docker.internal:9080 (Docker Desktop)
# 4. 使用宿主机IP: http://172.17.0.1:9080 (Linux Docker默认网桥)
API_BACKEND_URL=http://backend:9080

# 构建时的 API 基础路径（可选，默认为 /api）
# 这个路径用于前端代码中的 API 请求前缀
VITE_API_BASE_URL=/api